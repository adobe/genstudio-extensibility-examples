# App Directory

> **Do not modify `ExtensionRegistration.tsx` unless you know what you are doing.**

- `ExtensionRegistration.tsx` — registers all five extension points with the GenStudio host via `@adobe/uix-guest`
- `index.tsx` — React router, mounts all panel/dialog components at their routes
