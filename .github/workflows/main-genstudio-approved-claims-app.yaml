name: genstudio mlr claims app Main

on:
  push:
    branches:
      - main
    paths:
      - "genstudio-approved-claims-app/**"
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/aio-app-template.yml
    with:
      app: genstudio-approved-claims-app
      environment: |
        [
          "Genstudio Engineering 01 - genstudio-approved-claims-app - workspace Production",
          "GenStudio GA PAT04 - genstudio-approved-claims-app - workspace Production",
          "Genstudio - Engineering - genstudio-approved-claims-app - workspace Production"
        ]
    secrets: inherit
